# Payment configuration
ENABLE_PAYMENTS=true
FRONTEND_URL=https://ritt-ai-assistant-frontend.onrender.com

# Add this to your existing .env.local file to enable payments
# Make sure your STRIPE_SECRET_KEY is already set in .env.local
