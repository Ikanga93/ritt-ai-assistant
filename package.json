{
  "name": "ritt-drive-thru",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-deps": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm install tailwindcss postcss autoprefixer && npm run build || true && cd ../backend && npm run build",
    "start": "cd backend && node dist/agent.js"
  },
  "engines": {
    "node": ">=16"
  }
}
